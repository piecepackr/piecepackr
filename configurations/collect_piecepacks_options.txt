Usage: Program to arrange piecepacks


Options:
	-h, --help
		Show this help message and exit

	--collection_filename=COLLECTION_FILENAME
		(default "collection_filename")

	--collection_title=COLLECTION_TITLE
		(default "collection_title")

	--decks=DECKS
		(all of them)

	--paper=PAPER
		Paper size, either "letter" or "A4" (default "letter")

	--font=FONT
		Default font family (default "sans")


